"""
tool-permission-service

Tool registry + trust tiers + capability binding + per-call
authorization for every tool invocation. "No tool call without
authorization" — every /tools/authorize-call request produces a
security event on denial.
"""
